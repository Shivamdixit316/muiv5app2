import React from "react";
import { Button } from "@mui/material";
const CreateBtn = () => {
  return (
    <div>
      <Button color="purple">Material UI</Button>
    </div>
  );
};

export default CreateBtn;
